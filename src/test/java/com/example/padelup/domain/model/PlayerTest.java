package com.example.padelup.domain.model;

import com.example.padelup.entity.Player;
import io.vavr.collection.List;
import io.vavr.control.Validation;
import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.*;

class PlayerTest {

    @Test
    void validate() {
//        Validation<String, Player> foruzmand = Player.validate(null, "", "Foruzmand", "ali@sth.com", 0, 0, null);
//        assertTrue(foruzmand.isInvalid());
//        assertEquals("firstName can not be null or empty", foruzmand.getError());
    }

}